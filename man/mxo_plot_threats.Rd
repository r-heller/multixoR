% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mxo_plot_threats.R
\name{mxo_plot_threats}
\alias{mxo_plot_threats}
\title{Plot the threatened lines of a position}
\usage{
mxo_plot_threats(game, player = mxo_to_move(game), min_marks = 2L, ...)
}
\arguments{
\item{game}{An \code{mxo_game} object.}

\item{player}{Integer scalar, \code{1L} (X) or \code{2L} (O). Defaults to the
player to move.}

\item{min_marks}{Integer scalar; minimum \code{m} to count (default \code{2L}).}

\item{...}{Unused. Reserved for future arguments.}
}
\value{
A \code{ggplot} object showing m-line counts per axis class.
}
\description{
For each axis class (spatial / time / timeline / mixed), counts the
existence-gated extents where \code{player} has at least \code{min_marks} marks
and the rest are empty, and renders them as a labelled bar chart.
}
\examples{
g <- mxo_new_game()
g <- mxo_move(g, "present", 0L, 0L, 0L)
p <- mxo_plot_threats(g, player = 1L)
inherits(p, "ggplot")
}
