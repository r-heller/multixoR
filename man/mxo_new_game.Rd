% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mxo_game.R
\name{mxo_new_game}
\alias{mxo_new_game}
\title{Start a new multixoR game}
\usage{
mxo_new_game(
  n = 4L,
  d_spatial = 3L,
  k = 3L,
  ply_cap = 60L,
  max_timelines = 32L
)
}
\arguments{
\item{n}{Integer scalar, spatial side length. Default 4.}

\item{d_spatial}{Integer scalar, number of spatial dimensions. Default 3.}

\item{k}{Integer scalar, run length required to win. Default 3. Must satisfy
\code{k <= n}.}

\item{ply_cap}{Integer scalar, total plies allowed before the game is
declared a draw. Default 60.}

\item{max_timelines}{Integer scalar, maximum number of timelines the
multiverse may host. Default 32.}
}
\value{
An object of class \code{mxo_game} with one empty board at \verb{(L0, t0)}
and X to move.
}
\description{
Creates a fresh game state with one empty board at \verb{(L0, t0)}. The geometry
is parameterised by \verb{(n, d_spatial, k)}; the default 4 / 3 / 3 corresponds
to the canonical configuration described in the game specification.
}
\examples{
g <- mxo_new_game()
g
mxo_to_move(g)
}
