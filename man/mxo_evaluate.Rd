% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mxo_evaluate.R
\name{mxo_evaluate}
\alias{mxo_evaluate}
\title{Heuristic evaluation of a multixoR position}
\usage{
mxo_evaluate(
  game,
  player = mxo_to_move(game),
  w = NULL,
  w_timeline = 1,
  terminal_score = 1e+06
)
}
\arguments{
\item{game}{An \code{mxo_game} object.}

\item{player}{Integer scalar, the perspective player (\code{1L} for X, \code{2L} for
O). Defaults to the player to move.}

\item{w}{Numeric vector of length \code{k} giving the per-m-line weights. The
default is exponential: \verb{1, 8, 64, ...}.}

\item{w_timeline}{Numeric multiplier applied to m-lines whose direction
traverses the timeline axis (axis class \code{"timeline"} or \code{"mixed"}).
Default \code{1}. Larger values amplify cross-timeline tactics.}

\item{terminal_score}{Magnitude of the terminal sentinel. Default \code{1e6}.}
}
\value{
A numeric scalar.
}
\description{
Returns a numeric scalar; positive values favour \code{player}. Terminal states
map to a large signed sentinel scaled to prefer faster wins.
}
\examples{
g <- mxo_new_game()
mxo_evaluate(g)
}
