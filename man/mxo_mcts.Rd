% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mxo_mcts.R
\name{mxo_mcts}
\alias{mxo_mcts}
\title{Light UCT Monte-Carlo Tree Search}
\usage{
mxo_mcts(
  game,
  iterations = 1000L,
  c_uct = 1.4,
  rollout = c("heuristic", "random"),
  branch_policy = c("promising", "all", "none"),
  time_budget = NULL,
  rollout_depth = NULL,
  epsilon = 0.7,
  seed = NULL
)
}
\arguments{
\item{game}{An \code{mxo_game} object.}

\item{iterations}{Integer scalar, maximum MCTS iterations. Default \code{1000L}.}

\item{c_uct}{Exploration constant. Default \code{1.4}.}

\item{rollout}{One of \code{"heuristic"} (default, epsilon-greedy over a 1-ply
evaluator) or \code{"random"} (uniform legal).}

\item{branch_policy}{One of \code{"promising"} (default), \code{"all"}, \code{"none"}.
See \code{\link[=mxo_search]{mxo_search()}} for the trade-off.}

\item{time_budget}{Optional numeric. If supplied, stops once the elapsed
wall time exceeds this many seconds.}

\item{rollout_depth}{Maximum rollout depth in plies. Defaults to the
game's \code{ply_cap}.}

\item{epsilon}{Probability of taking the greedy move during a heuristic
rollout. Default \code{0.7}.}

\item{seed}{Optional integer seed for reproducible runs.}
}
\value{
An object of class \code{mxo_mcts_result}.
}
\description{
Runs \code{iterations} MCTS playouts (or stops earlier when \code{time_budget} is
exceeded) and returns a per-root-move summary.
}
\examples{
g <- mxo_new_game(n = 3L, k = 3L)
set.seed(1)
res <- mxo_mcts(g, iterations = 50L, branch_policy = "none")
res$move
}
