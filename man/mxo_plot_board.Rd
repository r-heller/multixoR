% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mxo_plot_board.R
\name{mxo_plot_board}
\alias{mxo_plot_board}
\title{Render a single multiverse board}
\usage{
mxo_plot_board(
  game,
  L = 0L,
  t = NULL,
  view = c("slices", "cube"),
  overlay = c("none", "top3", "heatmap"),
  rating = NULL,
  highlight_lines = NULL,
  ...
)
}
\arguments{
\item{game}{An \code{mxo_game}.}

\item{L, t}{Integer scalars selecting the board.}

\item{view}{One of \code{"slices"} (default ggplot2 facetted by z) or
\code{"cube"} (interactive plotly 3D scatter).}

\item{overlay}{One of \code{"none"}, \code{"top3"}, \code{"heatmap"}.}

\item{rating}{Optional rating tibble from \code{\link[=mxo_rate_moves]{mxo_rate_moves()}}. When
\code{NULL} and \code{overlay != "none"}, the rating is computed on the fly
with \code{method = "heuristic"}.}

\item{highlight_lines}{Optional list of 3-cell win-style lines to draw
on top of the board (each element a \code{list(L, t, idx)} triple).
Defaults to the win-line if the game is terminal.}

\item{...}{Unused. Reserved for future arguments.}
}
\value{
A \code{ggplot} (slices) or \code{plotly} (cube) object.
}
\description{
Render a single multiverse board
}
\examples{
g <- mxo_new_game()
g <- mxo_move(g, "present", 0L, 0L, 0L)
p <- mxo_plot_board(g, L = 0L, t = 0L)
inherits(p, "ggplot")
}
