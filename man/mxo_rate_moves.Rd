% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mxo_rate.R
\name{mxo_rate_moves}
\alias{mxo_rate_moves}
\title{Rate the legal moves of a position}
\usage{
mxo_rate_moves(
  game,
  method = c("heuristic", "search", "mcts"),
  depth = 2L,
  mcts_iter = 200L,
  branch_policy = c("promising", "all", "none"),
  ...
)
}
\arguments{
\item{game}{An \code{mxo_game} object.}

\item{method}{One of \code{"search"} (negamax depth lookup), \code{"mcts"}, or
\code{"heuristic"}. Default \code{"heuristic"} for speed; \code{"search"} and
\code{"mcts"} are slower but stronger.}

\item{depth}{Search depth when \code{method = "search"}. Default \code{2L}.}

\item{mcts_iter}{Iterations when \code{method = "mcts"}. Default \code{200L}.}

\item{branch_policy}{Branch-policy passed through to search/MCTS. Default
\code{"promising"}.}

\item{...}{Additional arguments forwarded to the chosen method.}
}
\value{
A type-stable tibble with columns \code{kind}, \code{L_src}, \code{t_src},
\code{idx}, \code{player}, \code{score}, \code{win_prob}, \code{rank}, \code{label}.
}
\description{
For each legal move, evaluate the resulting position from the mover's
perspective, attach a \code{win_prob}, derive a chess-engine-style label
(\code{"best"} / \code{"strong"} / \code{"ok"} / \code{"weak"} / \code{"blunder"}) from the
probability drop vs the best move, and rank the rows.
}
\examples{
g <- mxo_new_game(n = 3L, k = 3L)
head(mxo_rate_moves(g))
}
