% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mxo_search.R
\name{mxo_search}
\alias{mxo_search}
\title{Negamax with alpha-beta pruning}
\usage{
mxo_search(
  game,
  depth = 3L,
  branch_policy = c("promising", "all", "none"),
  w = NULL,
  w_timeline = 1,
  terminal_score = 1e+06
)
}
\arguments{
\item{game}{An \code{mxo_game} object.}

\item{depth}{Integer scalar, search depth in plies. Default \code{3L}.}

\item{branch_policy}{One of \code{"promising"} (default), \code{"all"}, or \code{"none"}.}

\item{w}{Optional weight vector of length \code{k} (see \code{\link[=mxo_evaluate]{mxo_evaluate()}}).}

\item{w_timeline}{Numeric multiplier for cross-timeline lines. Default \code{1}.}

\item{terminal_score}{Magnitude of terminal sentinel. Default \code{1e6}.}
}
\value{
A type-stable list with components:
\itemize{
\item \code{value} (dbl): the negamax value from the mover's perspective.
\item \code{move} (tibble): the best move (one row in legal-moves form) or a
zero-row tibble when the game is terminal.
}
}
\description{
Searches \code{depth} plies deep under a configurable branch-move policy and
returns the best move found along with its negamax value.
}
\examples{
g <- mxo_new_game(n = 3L, k = 3L)
res <- mxo_search(g, depth = 1L, branch_policy = "none")
res$value
}
